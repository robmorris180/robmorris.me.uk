.header
    grid-column: 2 / span 10;
    grid-row: 1
    display: flex;
    justify-content: space-between;
    align-items: flex-end;

.header__logo
    color: var(--black)
    font-size: 32px
    font-weight: 700
    letter-spacing: -1.5px
    line-height: 1